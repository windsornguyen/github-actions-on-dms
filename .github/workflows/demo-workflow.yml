# @generated by Hollywood. Do not edit by hand.

name: Demo on Dedalus Machine
on:
  workflow_dispatch: {}
  push:
    branches:
      - main
jobs:
  hello-from-dm:
    runs-on:
      - self-hosted
      - dedalus
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
      - name: Prove this ran on a Dedalus Machine
        run: |-
          echo "host: $(hostname)"
          uname -a
          cat /etc/os-release
          mount | grep dedalusfs
      - name: Teardown Dedalus Machine
        if: ${{ always() }}
        uses: ./.github/actions/dedalus/teardown-machine
        with:
          machine-id: ${{ vars.DM_MACHINE_ID }}
          dedalus-api-key: ${{ secrets.DEDALUS_API_KEY }}
          dedalus-base-url: ${{ vars.DEDALUS_BASE_URL }}
